/**
 * Código gerado pelo Kubb a partir do OpenAPI do backend.
 *
 * Configure a URL em `kubb.config.ts` (default: http://localhost:8000/api/schema/)
 * e rode: `npm run generate:api`
 *
 * Com o backend offline, a pasta permanece vazia até a primeira geração bem-sucedida.
 */
export {};
